#include <stdio.h>
#include <math.h>
 
double pierwiastek(double zmienna) {
  return sqrt(zmienna);
}

int main() {
  double pocz, konc, krok, temp;
  printf("Podaj wartość początkową, konćową oraz krok do argumentu.\n");
  scanf("%lf", &pocz);
  scanf("%lf", &konc);
  scanf("%lf", &krok);

  if (pocz > konc) {
    temp = pocz;
    pocz = konc; 
    konc = temp;
  }
  if(krok < .0) krok *= -1.0;
  while(pocz <= konc){
    if
      printf(" %.4lf  %.4lf\n", pocz, odwrotnosc(pocz));
    }
    
    pocz += krok;
  }

  return 0;
}
